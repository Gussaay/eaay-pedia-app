node_modules
dist
.env
.env.local
android/app/build
android/build
android/.gradle
*.keystore

# Old Sketchware Android project kept in this folder for reference only
# (not part of the web/Capacitor build).
/app/
/gradle/
/build.gradle
/settings.gradle
/local.properties
/.gradle/
/.idea/
# Local editor/session settings, but skills are shared with the repo
/.claude/launch.json
/.claude/settings.local.json
*.zip

# Local signing secrets (never commit)
keystore-secrets.txt
*.jks
